spring:
  application:
    name: backend-adocao


  datasource:
    url: jdbc:postgresql://localhost:5432/grcusuario
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
