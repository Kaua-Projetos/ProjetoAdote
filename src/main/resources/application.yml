spring:
  application:
    name: backend-adocao


  datasource:
    url: jdbc:postgresql://gerenciadorapi-database.ct8wo6og4nao.sa-east-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
